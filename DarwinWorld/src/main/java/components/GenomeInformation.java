package components;

public record GenomeInformation(int maxMutation,
                                int minMutation,
                                boolean slowEvolvingFlag,
                                int genomeLength) {
}
